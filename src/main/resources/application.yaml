server:
  port: 8080

spring:
  application:
    name: transaction-service
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:5432/transactiondb
    username: ${DB_USER:admin}
    password: ${DB_PASSWORD:secret123}
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

account:
  service:
    url: http://account-service:8080

management:
  endpoints:
    web:
      exposure:
        include: health,info